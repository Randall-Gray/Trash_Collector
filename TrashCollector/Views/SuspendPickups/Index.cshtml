@model IEnumerable<TrashCollector.Models.SuspendPickup>

@{
    ViewData["Title"] = "Customer: Suspend Pickup";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Customer Suspend Pickups</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <!--
            <th>
                @Html.DisplayNameFor(model => model.Completed)
            </th>
            -->
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <!--
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            -->
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <!--
            <td>
                @Html.DisplayFor(modelItem => item.Completed)
            </td>
            -->
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <!--
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerId)
            </td>
            -->
            <td>
                <a asp-action="Edit" asp-route-id="@item.SuspendPickupId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SuspendPickupId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SuspendPickupId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
