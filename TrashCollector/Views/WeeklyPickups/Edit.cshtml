@model TrashCollector.Models.WeeklyPickup

@{
    ViewData["Title"] = "Edit: Weekly Pickup";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Edit Weekly Pickup</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="WeeklyPickupId" />
            <!--
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="Completed" /> @Html.DisplayNameFor(model => model.Completed)
                </label>
            </div>
            -->
            <div class="form-group">
                <label asp-for="WeekDayId" class="control-label"></label>
                <select asp-for="WeekDayId" class="form-control" asp-items="ViewBag.WeekDayId"></select>
                <span asp-validation-for="WeekDayId" class="text-danger"></span>
            </div>
            <!--
            <div class="form-group">
                <label asp-for="CustomerId" class="control-label"></label>
                <select asp-for="CustomerId" class="form-control" asp-items="ViewBag.CustomerId"></select>
                <span asp-validation-for="CustomerId" class="text-danger"></span>
            </div>
            -->
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to Customer Weekly Pickup</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
